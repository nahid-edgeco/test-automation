# name: Test on Windows

# on:
#   push:
#     branches:
#       - main

# jobs:
#   build:
#     runs-on: self-hosted

#     steps:
#       - name: Checkout the code
#         uses: actions/checkout@v3

#       - name: Run the program
#         run: |
#           # cd E:\Developments\gitHubTest\automation\test-automation
#           # git pull
#           npm ci

name: Test on Windows

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Checkout the code
        uses: actions/checkout@v3

      - name: Set repository directory
        run: |
          cd E:\Developments\gitHubTest\automation\test-automation

      - name: Run the program
        run: |
          npm ci
