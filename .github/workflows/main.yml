name: Test on Windows

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Checkout the code
        uses: actions/checkout@v3
        # with:
        #   paths: E:\Developments\gitHubTest\automation\test-automation

      - name: Run the program
        run: |
          cd E:\Developments\gitHubTest\automation
          git pull
          npm ci
